<UserControl x:Class="VendingService.WPF.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Loaded="UserControl_OnLoaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock DockPanel.Dock="Left" Text="{Binding Title}" FontSize="24" FontWeight="SemiBold"/>
            <TextBlock DockPanel.Dock="Right" VerticalAlignment="Bottom" Foreground="#666"
                       Text="{Binding MachinesSummary}"/>
        </DockPanel>

        <Border Grid.Row="1" Background="White" CornerRadius="6" Padding="12" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0" Text="Торговый автомат"/>
                <ComboBox Grid.Column="1"
                          MinWidth="280"
                          ItemsSource="{Binding Machines}"
                          DisplayMemberPath="Name"
                          SelectedItem="{Binding SelectedMachine}"/>
                <Button Grid.Column="2" Width="110" Height="30" Margin="12,0,0,0"
                        Content="Обновить" Command="{Binding RefreshCommand}"/>
            </Grid>
        </Border>

        <Border Grid.Row="2" Background="White" CornerRadius="6" Padding="12">
            <Grid>
                <DataGrid ItemsSource="{Binding Items}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          AlternationCount="2"
                          AlternatingRowBackground="#F7F7F7"
                          HeadersVisibility="Column"
                          GridLinesVisibility="None">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Товар" Binding="{Binding ProductName}" Width="*"/>
                        <DataGridTextColumn Header="В наличии" Binding="{Binding QuantityOnHand}" Width="120"/>
                        <DataGridTextColumn Header="Мин. запас" Binding="{Binding MinimumStock}" Width="120"/>
                        <DataGridTextColumn Header="Средние продажи" Binding="{Binding AverageDailySales, StringFormat={}{0:N2}}" Width="150"/>
                        <DataGridTextColumn Header="Обновлено" Binding="{Binding UpdatedAt, StringFormat={}{0:dd.MM.yyyy HH:mm}}" Width="150"/>
                    </DataGrid.Columns>
                </DataGrid>

                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center"
                           Foreground="#666" FontSize="14"
                           Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibilityConverter}}"
                           Text="{Binding EmptyHintText}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
